#include "map_renderer.h"

MapRenderer::MapRenderer(RenderSets rs) : render_sets_(rs) {}

RenderSets MapRenderer::MakeRender(svg::Point& point ) const {
    // svg::Polyline line;
    // line.AddPoint().
}

